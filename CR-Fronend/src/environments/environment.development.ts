/**
 * Desarrollo — lo que usa `ng serve` en tu máquina.
 *
 * Acá sí va la dirección completa: el servidor de Angular vive en
 * el 4200 y Laravel en el 8000, son dos orígenes distintos y el
 * navegador necesita saber a cuál llamar.
 */
export const environment = {
  production: false,
  apiUrl: 'http://127.0.0.1:8000/api'
};
